import Link from "next/link";
import { auth } from "@/auth";
import { getPrisma } from "@/lib/db";
import { listPaymentReviews } from "@/lib/payment-review";

export const dynamic = "force-dynamic";
export default async function PaymentReviewsPage() { const session = await auth(); if (!session?.user) return <main className="admin-placeholder"><Link href="/dang-nhap">Đăng nhập</Link></main>; if (session.user.role !== "ADMIN") return <main className="admin-placeholder"><h1>Không có quyền.</h1></main>; const prisma = getPrisma(); if (!prisma) return <main className="admin-placeholder"><h1>Không thể tải payment review.</h1></main>; const items = await listPaymentReviews(prisma).catch(() => null); if (!items) return <main className="admin-placeholder"><h1>Không thể tải payment review.</h1></main>; return <main className="admin-placeholder"><Link href="/admin/products">← Products</Link><p className="eyebrow">ADMIN / PAYMENT REVIEW</p><h1>MoMo cần kiểm tra</h1><p className="muted">Không tự động hoàn tiền hoặc gọi API hoàn tiền.</p><div className="admin-order-list">{items.length === 0 ? <p className="admin-note">Không có payment review đang chờ.</p> : items.map((item) => <Link className="admin-order-card" key={item.id} href={`/admin/payment-reviews/${item.id}`}><div><strong>{item.code}</strong><span>{item.customerName} · {item.customerPhone ?? "—"}</span><small>{item.guestEmail ?? "—"}</small></div><div><strong>{item.total.toLocaleString("vi-VN")} ₫</strong><span>REVIEW_REQUIRED · {item.payments[0]?.providerTransactionId ?? "Chưa có transaction ID"}</span></div></Link>)}</div></main>; }
