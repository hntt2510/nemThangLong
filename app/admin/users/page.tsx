import { getPrisma } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function UsersPage() { const prisma = getPrisma(); if (!prisma) return <section className="ops-empty-state"><h2>Database chưa sẵn sàng</h2></section>; const users = await prisma.user.findMany({ select: { id: true, name: true, email: true, phone: true, role: true, createdAt: true, orders: { select: { total: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <div className="ops-stack"><section className="ops-page-heading"><div><p className="ops-overline">KHÁCH HÀNG</p><h2>Khách hàng</h2><p>Danh sách an toàn: không hiển thị mật khẩu, không đăng nhập thay người dùng.</p></div></section><section className="ops-card ops-table-card"><div className="ops-data-table"><div className="ops-data-head"><span>Khách hàng</span><span>Vai trò</span><span>Đơn hàng</span><span>Tổng chi tiêu</span><span>Ngày tạo</span><span /></div>{users.map((user) => <div className="ops-data-row" key={user.id}><span><b>{user.name ?? "Chưa đặt tên"}<small>{user.email} · {user.phone ?? "—"}</small></b></span><span><i className="ops-badge">{user.role}</i></span><span>{user.orders.length}</span><span>{user.orders.reduce((sum, order) => sum + order.total, 0).toLocaleString("vi-VN")} ₫</span><span>{user.createdAt.toLocaleDateString("vi-VN")}</span><span>—</span></div>)}</div></section></div>; }
