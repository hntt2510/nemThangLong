import { getPrisma } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function CategoriesPage() { const prisma = getPrisma(); if (!prisma) return <section className="ops-empty-state"><h2>Database chưa sẵn sàng</h2></section>; const items = await prisma.productCategory.findMany({ include: { _count: { select: { products: true } } }, orderBy: [{ sortOrder: "asc" }, { name: "asc" }] }); return <div className="ops-stack"><section className="ops-page-heading"><div><p className="ops-overline">SẢN PHẨM</p><h2>Loại sản phẩm</h2><p>Thứ tự hiển thị catalog được điều khiển tại đây.</p></div></section><section className="ops-card ops-table-card"><div className="ops-data-table"><div className="ops-data-head"><span>Tên</span><span>Trạng thái</span><span>Số sản phẩm</span><span>Thứ tự</span><span /></div>{items.map((item) => <div className="ops-data-row ops-category-row" key={item.id}><span><b>{item.name}<small>{item.slug}</small></b></span><span><i className={`ops-badge ${item.status.toLowerCase()}`}>{item.status === "PUBLISHED" ? "ĐÃ XUẤT BẢN" : "NHÁP"}</i></span><span>{item._count.products}</span><span>{item.sortOrder}</span><span>Chỉnh sửa →</span></div>)}</div></section></div>; }
